package com.srmcem.payroll.enums;

/**
 * Employment status of an employee.
 *
 * <p>The dashboard query expects the stored value to be either
 * {@code 'ACTIVE'} or {@code 'INACTIVE'} (stored as the enum name).
 */
public enum EmployeeStatus {
    ACTIVE,
    INACTIVE
}
